# nvim 内では nvim --server 経由で親 nvim にファイルを開かせたいので、専用の設定を用意する
# lazygit を立ち上げるときに追加でこのファイルを読み込むことで一部の設定のみを上書きできる
# https://github.com/jesseduffield/lazygit/blob/master/docs/Config.md#overriding-default-config-file-location
#
# <C-\><C-N> で terminal モード抜け → <C-w>p で前ウィンドウへ → :edit でファイルを開く

os:
  edit: nvim --server "$NVIM" --remote-send '<C-\><C-N><C-w>p:edit {{filename}}<CR>'
  editAtLine: nvim --server "$NVIM" --remote-send '<C-\><C-N><C-w>p:edit +{{line}} {{filename}}<CR>'
  editAtLineAndWait: nvim +{{line}} {{filename}}
  openDirInEditor: nvim --server "$NVIM" --remote-send '<C-\><C-N><C-w>p:edit {{dir}}<CR>'
  editInTerminal: false # nvim 内だけの使用ならfalse の方が良い
